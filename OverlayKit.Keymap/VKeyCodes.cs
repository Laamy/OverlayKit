namespace OverlayKit.Keymap
{
    public enum VKeyCodes
    {
        KeyDown = 0, // keydown event (key pressed)
        KeyUp = 1, // keyup event (key released)
        KeyHeld = 2, // key held (continuously pressed/held)
    }
}